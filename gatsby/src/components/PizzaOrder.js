 import React from 'react';
import usePizza from '../utils/usePizza';

 export default function PizzaOrder({ 
     order, 
     pizzas,
     removeFromOrder,
     plainImage
 }) {
    return (
        <>
            <p>Order!!!!!!!</p>
        </>
    )
 }