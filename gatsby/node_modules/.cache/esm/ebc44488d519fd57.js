let path;_d51‍.x([["createPages",()=>createPages]]);_d51‍.w("path",[["default",["path"],function(v){path=v}]]);

async function turnPizzasIntoPages({graphql, actions}) {
    //1. get a page template
    const pizzaTemplate = path.resolve('./src/templates/PizzaTemplate.js');
    //2. query all pizzas
    const {data} = await graphql(`
        query {
            pizzas: allSanityPizza {
                nodes {
                    name
                    slug {
                        current
                    }
                }
            }
        }
    `);
    _d51‍.g.console.log({data});
    //3. loop over pizzas and create a page for each
    data.pizzas.nodes.forEach(pizza => {
        _d51‍.g.console.log('creating', pizza.name);
        
    });
}

       async function createPages(params) {
    //create pages dynamically
    //1. PIZZAS
    await turnPizzasIntoPages(params);
    //2. TOPPINGS
    //3. SLICEMASTERS
}